<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mid.login.repo.UserDAO">

    <!-- 회원 등록 -->
    <insert id="insertUser" parameterType="com.mid.login.model.UserInfoVO">
        INSERT INTO MEMBER (
            
            mem_name,
            mem_email,
            mem_id,
            mem_pwd,
            mem_addr,
            mem_tel,
            mem_regdate
        ) VALUES (
            
            #{memName},
            #{memEmail},
            #{memId},
            #{memPwd},
            #{memAddr},
            #{memTel},
            SYSDATE
        )
    </insert>

    <!-- 나머지 부분은 그대로 유지 -->
    <select id="getUserInfo" resultType="com.mid.login.model.UserInfoVO">
        SELECT 
            
            mem_name AS memName,
            mem_email AS memEmail,
            mem_id AS memId,
            mem_pwd AS memPwd,
            mem_addr AS memAddr,
            mem_tel AS memTel,
            mem_regdate AS memRegdate
        FROM MEMBER
        WHERE mem_id = #{memId}
        AND mem_pwd = #{memPwd}
    </select>
	
	<!-- 아이디 중복 체크 -->
    <select id="idCheck" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM MEMBER
        WHERE mem_id = #{memId}
    </select>

    <!-- 마지막 로그인 날짜 업데이트 -->
   <!--  <update id="updateLastLogin" parameterType="string">
        UPDATE USER_INFO
        SET LAST_LOGIN = SYSDATE
        WHERE USER_ID = #{userId}
    </update> -->

</mapper>
